{
  "name": "nudgley",
  "version": "2.0.0-migration.1",
  "private": true,
  "description": "Voice-first ADHD task board with embedded coaching.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:syntax": "tsc -b --pretty false"
  },
  "dependencies": {
    "vite": "^7.0.0",
    "typescript": "^5.8.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
