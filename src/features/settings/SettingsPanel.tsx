import { useAppDispatch, useAppState } from '../../app/providers/AppProviders';

export function SettingsPanel() {
  const state = useAppState();
  const dispatch = useAppDispatch();
  if (!state.ui.settingsOpen) return null;
  return <section className="modal-backdrop" role="dialog" aria-modal="true" aria-labelledby="settings-title"><div className="sheet settings-sheet"><header><h2 id="settings-title">Settings & profile</h2><button onClick={() => dispatch({ type: 'toggleUi', key: 'settingsOpen', value: false })}>Close</button></header><section><h3>Profile</h3><label>Name <input value={state.profile.name ?? ''} onChange={(event: any) => dispatch({ type: 'saveProfile', profile: { ...state.profile, name: event.target.value } })} /></label><label>Tone <select value={state.profile.tone} onChange={(event: any) => dispatch({ type: 'saveProfile', profile: { ...state.profile, tone: event.target.value as typeof state.profile.tone } })}><option value="direct">Direct</option><option value="balanced">Balanced</option><option value="supportive">Supportive</option></select></label></section><section><h3>Nudges</h3><label>Frequency <select value={state.settings.notifications.frequency} onChange={(event: any) => dispatch({ type: 'saveSettings', settings: { ...state.settings, notifications: { ...state.settings.notifications, frequency: event.target.value as typeof state.settings.notifications.frequency, enabled: event.target.value !== 'off' } } })}><option value="off">Off</option><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option></select></label></section><section><h3>Privacy</h3><p>Your board data is stored in this browser first. AI and remote storage are isolated behind services so they can be hardened without rewriting the UI.</p></section></div></section>;
}
